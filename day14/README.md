Execute this code via `cargo run <filename> <num_cols> <num_rows> <seconds>`.

### Observations

1. In Rust, `%` is the reminder operator and `rem_euclid` is the modulus
   operator.  These are not the same when negative numbers are involved.
